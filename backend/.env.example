# Server Configuration
PORT=3001
NODE_ENV=development

# OpenAI Configuration (Primary Provider)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Alternative AI Providers (Fallback Options)
# Configure at least one alternative provider for automatic fallback
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# AI Provider Priority (comma-separated list)
# Options: openai, gemini
# Default: openai,gemini
AI_PROVIDER_PRIORITY=openai,gemini

# CORS Configuration (optional)
# CORS_ORIGIN=http://localhost:5173
